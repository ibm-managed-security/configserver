spring:
  cloud:
    config:
      server:
        git:
          force_pull: true
          uri: file:/opt/configserver
          searchPaths:
            - config-store/**
management:
    security:
      enabled: false
